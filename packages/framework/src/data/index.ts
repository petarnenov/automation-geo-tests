/**
 * `@geowealth/e2e-framework/data` — static seed data and factories.
 *
 * Phase 2 step 2 (D-37). Today only constants. Phase 2 step 2 also
 * adds factories (`FirmFactory`, `ProspectFactory`) on top of the
 * API client; those land in their own commit alongside their first
 * consumers.
 */

export * from './constants/index';
