/**
 * `@geowealth/e2e-framework/types` — shared TypeScript types mirroring
 * GeoWealth Java entities.
 *
 * Phase 0 Step 0.F: empty stub. Phase 2 fills this with `Firm`,
 * `Account`, `User`, `Strategy`, etc. as the API client wrappers grow.
 */

export {};
