/**
 * `@geowealth/e2e-framework/helpers` — generic utilities.
 *
 * Phase 0 Step 0.F: empty stub. Phase 2 fills this with waits, retry,
 * uuid, and `withCdpClick` (Decision: CDP access encapsulated in a
 * single helper).
 */

export {};
