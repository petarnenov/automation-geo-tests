import { test } from '@geowealth/e2e-framework/fixtures';

test('@regression @billing-servicing C25789 Bucket Exclusions - Set All Accts to I = Y propagates to all accounts', async ({ workerFirmAdminPage, workerFirm }) => {
  test.fixme();
});
