import { test } from '@geowealth/e2e-framework/fixtures';

test('@regression @billing-servicing C25790 Bucket Exclusions - Set All Accts to I = N: HH-level upload accepted', async ({ workerFirmAdminPage, workerFirm }) => {
  test.fixme();
});
