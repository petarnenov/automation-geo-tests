import { test } from '@geowealth/e2e-framework/fixtures';

test('@regression @billing-servicing C25793 Bucket Exclusions - error scenarios (HH+Account same row, empty data, numeric EXCLUDED)', async ({ workerFirmAdminPage, workerFirm }) => {
  test.fixme();
});
