import { test } from '@geowealth/e2e-framework/fixtures';

test('@regression @billing-servicing C25379 Bucket Exclusions - wrong file format triggers validation', async ({ workerFirmAdminPage, workerFirm }) => {
  test.fixme();
});
