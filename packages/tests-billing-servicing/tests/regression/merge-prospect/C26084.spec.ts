import { test } from '@geowealth/e2e-framework/fixtures';

test('@regression @billing-servicing C26084 Merge Prospect - client with same-named document, site 1', async ({ workerFirmAdminPage, workerFirm }) => {
  test.fixme();
});
