import { test } from '@geowealth/e2e-framework/fixtures';

test('@regression @billing-servicing C26083 Merge Prospect - client with accounts + prospect with data, site 1', async ({ workerFirmAdminPage, workerFirm }) => {
  test.fixme();
});
