import { test } from '@geowealth/e2e-framework/fixtures';

test('@regression @billing-servicing C26058 Merge Prospect - client with accounts + prospect with data, site 61', async ({ workerFirmAdminPage, workerFirm }) => {
  test.fixme();
});
