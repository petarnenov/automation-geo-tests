import { test } from '@geowealth/e2e-framework/fixtures';

test('@regression @billing-servicing C26075 Unmanaged Assets - Remove All (RA action) clears all exclusion records', async ({ workerFirmAdminPage, workerFirm }) => {
  test.fixme();
});
