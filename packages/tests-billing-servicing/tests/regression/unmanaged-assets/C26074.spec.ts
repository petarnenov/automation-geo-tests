import { test } from '@geowealth/e2e-framework/fixtures';

test('@regression @billing-servicing C26074 Unmanaged Assets - Delete (D action) removes an existing exclusion record', async ({ workerFirmAdminPage, workerFirm }) => {
  test.fixme();
});
