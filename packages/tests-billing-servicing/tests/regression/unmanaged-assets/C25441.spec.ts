import { test } from '@geowealth/e2e-framework/fixtures';

test('@regression @billing-servicing C25441 Unmanaged Assets - template added and uploaded via drag and drop', async ({ workerFirmAdminPage, workerFirm }) => {
  test.fixme();
});
