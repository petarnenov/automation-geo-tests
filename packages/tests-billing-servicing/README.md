# `@geowealth/tests-billing-servicing` (placeholder)

> **Status:** Phase 0 Step 0.A placeholder.
>
> The bootstrap content is generated in Phase 0 Step 0.G from the scaffold templates by `packages/tooling/scripts/expand-templates.ts` running with `slug=billing-servicing`. Phase 1's `npm run scaffold:team` CLI invokes the same template expansion plus CODEOWNERS / migration tracker / CI matrix mutations.
>
> Per D-25, this package owns the entire current POC scope: `account-billing/`, `billing-specs/`, `create-account/`, `bucket-exclusions/`, `unmanaged-assets/`, `merge-prospect/`, `auto-link/`. Phase 4 ports these areas one at a time from `packages/legacy-poc/tests/<area>/` into `packages/tests-billing-servicing/tests/regression/<area>/` under the parity gate.
