# `@geowealth/legacy-poc` (placeholder)

> **Status:** Phase 0 Step 0.A placeholder.
>
> Step 0.B is a single-PR pure rename that moves the existing POC content into this directory:
>
> - `tests/` → `packages/legacy-poc/tests/`
> - `reporters/` → `packages/legacy-poc/reporters/`
> - `playwright.config.js` → `packages/legacy-poc/playwright.config.js` (kept as `.js` per D-31)
> - `scripts/` → `packages/legacy-poc/scripts/`
> - `testrail.config.json` → `packages/legacy-poc/testrail.config.json`
>
> The legacy POC keeps running from this new location through Phases 0–4. It is deleted entirely at Phase 5 sunset (after archiving the JS helpers as `docs/historical/legacy-poc-helpers.tar.gz`).
